This game uses a code-driven art pipeline.

Sprites, icons, effects, and the title crest are generated from `src/memory/assetManifest.ts` at runtime by the renderer. The pipeline keeps all visuals local, original, lightweight, and easy to retune without external art dependencies.
