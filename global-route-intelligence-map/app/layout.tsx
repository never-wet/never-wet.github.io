import type { Metadata } from "next";
import "maplibre-gl/dist/maplibre-gl.css";
import "./globals.css";

export const metadata: Metadata = {
  title: "Global Route Intelligence Map | Never Wet",
  description:
    "Interactive real-world map visualizing global flight paths, shipping routes, historical routes, trade corridors, and strategic chokepoints."
};

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
