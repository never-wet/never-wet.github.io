export const audioManifest = {
  enabled: false,
  profile: 'silent-lab',
  cues: {
    hover: false,
    select: false,
    trainComplete: false,
  },
  note: 'Audio is intentionally disabled so the lab stays focused and low-noise by default.',
} as const
