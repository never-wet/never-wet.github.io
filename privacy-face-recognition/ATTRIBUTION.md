# Attribution

This app uses `face-api.js` and its pretrained model files from the
`justadudewhohacks/face-api.js` project, distributed under the MIT license.

Only numeric face descriptors are stored by the app. Camera frames and raw face
images are not saved.
